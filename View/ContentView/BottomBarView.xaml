<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VisioTrainer.View.BottomBarView"
             BackgroundColor="{StaticResource CardBackgroundColor}">
    <!-- Lower menu bar buttons -->
    <HorizontalStackLayout Spacing="20"
                           HorizontalOptions="Center"
                           VerticalOptions="EndAndExpand">

        <!-- Home -->
        <Button Text="🏠"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                Clicked="HomeButton_OnClicked"/>

        <!-- Open quizmenu Button -->
        <Button Text="🔬"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30" 
                Clicked="QuizButton_OnClicked"/>

        <!-- Ranking menu Button -->
        <Button Text="👑"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30" 
                Clicked="RankingButton_OnClicked"/>

        <!-- View mistakes Button -->
        <Button Text="❌"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30" 
                Clicked="ViewMistakesButton_OnClicked"/>

    </HorizontalStackLayout>
</ContentView>